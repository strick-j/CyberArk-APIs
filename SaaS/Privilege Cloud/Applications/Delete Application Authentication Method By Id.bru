meta {
  name: Delete Application Authentication Method By Id
  type: http
  seq: 7
}

delete {
  url: https://{{identityTenantName}}.privilegecloud.cyberark.cloud/passwordvault/WebServices/PIMServices.svc/Applications/{{aamAppId}}/Authentications/{{aamAuthMethodId}}
  body: json
  auth: none
}

body:json {
  {
    "authentication":
    {
      "AuthType":"path", // <path|osuser|hash|machineAddress>
      "AuthValue": "C:\\Users\\example\\documents", // "<Path string>|<OSUser Name>|<Hash Value>|<Machine Address|CIDR>",
      "IsFolder": true, // <true|false>
      "AllowInternalScripts": true // <true|false>
    }
  }
}

vars:pre-request {
  aamAppId: SNow
  aamAuthMethodId: 3
}

assert {
  res.status: eq 200
}

script:pre-request {
  // Perform authentication usings platformToken.js tools
  const platformTokenAuth = require('./tools/platformTokenAuth');
  await platformTokenAuth.login();
}

docs {
  This method deletes a specific authentication method from a defined application.
  
  The user requires the following permission in the Vault:
  Manage Users
  
  
  
  
}
